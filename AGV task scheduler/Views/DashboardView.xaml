<UserControl x:Class="AGV_task_scheduler.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AGV_task_scheduler.Views" xmlns:components="clr-namespace:AGV_task_scheduler.Components" xmlns:viewmodels="clr-namespace:AGV_task_scheduler.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="AGV Task Scheduler" Margin="20" FontSize="20" Grid.Column="0"/>
            <Button Grid.Column="1" Content="Show Completed Tasks" Margin="20 20 20 20" Command="{Binding OpenTasksRunViewCommand}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="196*"/>
                <ColumnDefinition Width="71*"/>
                <ColumnDefinition Width="533*"/>
            </Grid.ColumnDefinitions>
            <components:AGVPanel Grid.Column="0" Margin="20,0,10,10" Grid.ColumnSpan="2" DataContext="{Binding AGVPanelViewModel}"/>
            <components:TasksPanel Grid.Column="2" Margin="10,0,20,10" DataContext="{Binding TaskPanelViewModel}"/>
        </Grid>
        <components:Taskbar Grid.Row="2"/>
    </Grid>
</UserControl>
