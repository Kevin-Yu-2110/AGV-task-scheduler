<Window x:Class="AGV_task_scheduler.Components.AssignTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AGV_task_scheduler.Components" xmlns:viewmodels="clr-namespace:AGV_task_scheduler.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AssignTaskViewModel}"
        mc:Ignorable="d"
        Title="AssignTaskView" Height="400" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" Margin="20 10 20 10" ItemsSource="{Binding AvailableAGVs}" AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedAGV, Mode=OneWayToSource}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding Id}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Assign" Grid.Row="1" Margin="20 0 20 10" Command="{Binding AssignAGVCommand}"/>
    </Grid>
</Window>
