<Window x:Class="AGV_task_scheduler.Components.CreateTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AGV_task_scheduler.Components" xmlns:viewmodels="clr-namespace:AGV_task_scheduler.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:CreateTaskViewModel}"
        mc:Ignorable="d"
        Title="CreateTaskView" Height="200" Width="600">
    <StackPanel>
        <TextBlock Text="Create Task" FontSize="25" Padding="10"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Task Description:" Grid.Column="0" Padding="10"/>
            <TextBox Grid.Column="1" VerticalContentAlignment="Center" Margin="0 0 10 0" Text="{Binding TaskDescription, Mode=TwoWay}"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="Add" Grid.Column="1" Padding="10" Margin="0 20 20 10" Command="{Binding AddTaskCommand}"/>
        </Grid>
    </StackPanel>
</Window>
