<Window x:Class="AGV_task_scheduler.Components.TasksRunView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AGV_task_scheduler.Components"
        mc:Ignorable="d"
        Title="TasksRunView" Height="400" Width="750">
    <StackPanel>
        <TextBlock Text="Completed Tasks" HorizontalAlignment="Center" FontSize="25"/>
        <DataGrid ItemsSource="{Binding TaskRuns}" IsReadOnly="True" AutoGenerateColumns="False" Margin="10 10 10 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Assigned AGV" Width="*" Binding="{Binding AssignedAGVId}"/>
                <DataGridTextColumn Header="Task Description" Width="5*" Binding="{Binding TaskDescription}"/>
                <DataGridTextColumn Header="Time Completed" Width="2*" Binding="{Binding CompletedTimeStamp}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>
